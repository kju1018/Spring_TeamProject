<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.ProductQnasDao">
	<select id="pQanSelectAll" resultType="productqnas">
		select boardno, userid, productno, btitle, uindex, bdate, bcontent, bcount, board_origin, board_re
		from productqnas
		order by boardno desc
	</select>
	<insert id="pQnaInsert" parameterType="productqnas">
		
	</insert>
</mapper>