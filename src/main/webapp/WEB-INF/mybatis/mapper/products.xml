<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.webapp.dao.ProductsDao">
<select id="pSelectAll" resultType="products">
	select productno, pcategory, pname, pprice, pregisterdate, psalescount, pstock, penable, datailimgoname, detailimgsname, detailimgtype
	from products
	ordey by productno desc
</select>

<insert id="pInsert" parameterType="products">
	<selectKey keyProperty="productno" resultType="int" order="BEFORE"> 
		   		select seq_productno.nextval from dual
	</selectKey>
	insert into products
	(productno, pcategory, pname, pprice, pregisterdate, psalescount, pstock, penable, datailimgoname, detailimgsname, detailimgtype)
	values
	(seq_productno.nextval, #{pcategory}, #{pname}, #{pprice}, sysdate, #{psalescount}, #{pstock}, #{penable}, #{datailimgoname}, #{detailimgsname}, #{detailimgtype})
</insert>

<update id="pUpdate" parameterType="int">
	update products set
	pcategory=#{pcategory}, 
	pname=#{pname}, pprice=#{pprice}, psalescount=#{psalescount}, 
	pstock=#{pstock}, penable=#{penable}, 
	datailimgoname=#{datailimgoname}, detailimgsname=#{detailimgsname}, detailimgtype=#{detailimgtype}
</update>

<select id="pSelectByPno" parameterType="int" resultType="products">
	select productno, pcategory, pname, pprice, pregisterdate, psalescount, pstock, penable, datailimgoname, detailimgsname, detailimgtype
	from products
	where productno=#{productno}
</select>

<delete id="pDeleteByPno" parameterType="int">
	delete from products wehre productno=#{productno}
</delete>
</mapper>